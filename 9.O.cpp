#include<stdio.h>
#include <stdlib.h>
int main(){

	FILE *out_file = fopen("1.O.c", "w");
           
          if (out_file == NULL || out_file == NULL) 
            {   
              printf("Error! Could not open file\n"); 
              exit(-1);
            } 
}
